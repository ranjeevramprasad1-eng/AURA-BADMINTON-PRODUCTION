/**
 * Display Timer Constants
 * Centralized configuration for display timing
 */
export const DISPLAY_TIMER_CONFIG = {
  SCORE_DURATION: 30000,      // 30 seconds - score full screen
  GROUP_DURATION: 10000,       // 10 seconds per group
  STANDINGS_DURATION: 10000,  // 10 seconds - fallback duration
  PROGRESS_UPDATE_INTERVAL: 100, // 100ms - progress update interval
};
